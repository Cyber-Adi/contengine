Design references live here (the juju.branding grid, the orange/cream editorial set).

NOT to copy. Those palettes violate Design System 3.8 — acid green, 3D renders,
photography. They are kept as EVIDENCE for the structural moves already implemented:

  - swipe affordance on every non-final slide (both references carry one; fond had none,
    and swipe-through to slide 3 is the metric Strategy v2 optimises for)
  - one-word accent pop inside a headline  ->  **word**
  - bracketed micro-label, top-right
  - flat geometric ornament
  - colour-split hero number ("2.4K" with the unit in accent)
  - hand-drawn circle annotation  ->  ((word))
